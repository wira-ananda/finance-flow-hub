{
  "name": "api",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf dist",
    "build": "rm -rf dist && tsc && cp appsscript.json dist/appsscript.json",
    "push": "bun run build && clasp push",
    "push:force": "bun run build && clasp push --force"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/google-apps-script": "^2.0.12",
    "typescript": "^7.0.2"
  }
}
